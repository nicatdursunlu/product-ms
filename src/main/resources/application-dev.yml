spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/product-ms
    username: postgres
    password: 123456
  jpa:
    hibernate:
        ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: always
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER

product-url: /api/v1/products
